# Runtime secrets are intentionally not committed. Configure these as environment
# variables or copy this file to secrets.toml and adapt app integration if needed.
# LLM_API_KEY = "..."
# LLM_MODEL = "your-model-name"
# LLM_API_BASE = "https://api.openai.com/v1"
